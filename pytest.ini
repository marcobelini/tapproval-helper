[pytest]
# Only the suite, and only the root: `testpaths = .` walked design/ and its
# node_modules trees, which is slow and finds nothing.
testpaths = test_claude_risk_classifier.py WatchApp/asc/test_asc_tooling.py
addopts = -v
